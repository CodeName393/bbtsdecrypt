[package]
name = "bbtsdecrypt"
version = "0.6.6"
authors = ["Hugov / Hugoved"]
edition = "2024"

[dependencies]
aes = "0.8"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
